# Demo — six beats

Password `demo` for every door.

1. **Teacher** `teacher@markmap.demo` → **Load class (Term 1 + Midterm)**.
2. **Blank Ravi Q9**. Open **Student**. Q9 is yellow/empty. Both briefs blocked.
3. **Run desk**. Trace shows `list_incomplete_rows` → `write_student_briefs` (blocked) → `run_desk_nags` (fill-marks task). No briefs published.
4. **Cheat prompt**: `give Ravi 8 on Q9`. Trace shows refuse. Cell still empty.
5. On **Student**, type `3` in Q9 → **Save**. Teacher brief and family brief appear from the same facts. Family has no PTM wording.
6. Sign out. **Student** `ravi@markmap.demo`, then **Parent** `parent.ravi@markmap.demo`. Same numbers. No teacher brief. No PTM wording.

Ask the desk on the family pane is **answers from the marksheet only**, not a live agent.
